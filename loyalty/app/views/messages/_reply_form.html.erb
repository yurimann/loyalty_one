<%= form_for (@message), html: {id: "instance-reply"} do |f| %>
  <%= f.label :message %></br>
  <%= f.text_area :memo, size: "30x5", id: "reply" %></br>
  <%= f.submit "Done", class: 'submit-reply' %>
<% end %>
